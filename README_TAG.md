# TAG 명령어

commit 을 진행한 후, 특정 커밋에 영구적인 이름표를 붙여 버전을 관리합니다.

- git tag -a <태그명> -m "<메시지>"
- git push origin <태그명>

태그명 에 해당하는 태그를 현재 커밋에 붙이며, 관련 메시지를 저장합니다.
이후 태그명으로 push하면, GitHub의 Release 탭에 해당 태그(대체적으로 버전 정보를 남김)으로 하여금
공식 릴리스를 생성합니다.

